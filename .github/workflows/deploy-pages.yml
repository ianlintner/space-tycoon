# GitHub Pages deployment is superseded by deploy-azure.yml which deploys
# to space-tycoon.cat-herding.net via Azure Static Web Apps.
# This file is kept disabled (no trigger) so it can be re-enabled if needed.
#
# To re-enable: restore the `on:` block below.
#
# on:
#   push:
#     branches: [main]
#   workflow_dispatch:
